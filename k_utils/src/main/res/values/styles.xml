<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:typeface">serif</item>

        <!-- Dialog Theming attributes -->
        <item name="md_corner_radius">6dp</item>
        <!--<item name="md_font_title">@font/monaco</item>-->
        <!--<item name="md_font_body">@font/monaco</item>-->
        <!--<item name="md_font_button">@font/monaco</item>-->
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar.FullScreen">
        <item name="android:windowFullscreen">true</item>
    </style>

    <!--Item点击效果 LinearLayout-->
    <style name="item_select">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">12dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:background">@drawable/selector_item_ll</item>
    </style>

    <style name="main_button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@color/color_main</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!--设备信息标题栏下样式-->
    <style name="deviceStyle">
        <item name="android:background">@drawable/bg_white_radius</item>
        <item name="android:textColor">@color/gray_f2f2f2</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingLeft">5dp</item>
        <item name="android:paddingRight">5dp</item>
        <item name="android:textSize">12sp</item>
    </style>

    <!--验证码按钮样式-->
    <style name="captcha">
        <item name="android:background">@drawable/captcha_select</item>
        <item name="android:textColor">@drawable/textcolor_captcha_selecter</item>
        <item name="android:textSize">12sp</item>
    </style>


    <!--dialog背景-->
    <style name="selectorDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 模糊 -->
        <item name="android:backgroundDimAmount">0.6</item>
    </style>

    <style name="style_dialog_base" parent="Base.Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!--dialog缩放动画-->
    <style name="dialog_anim_zoom">
        <item name="android:windowEnterAnimation">@anim/dialog_scale_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_scale_out</item>
    </style>
    <!--dialog底部动画-->
    <style name="dialog_anim_bottom">
        <item name="android:windowEnterAnimation">@anim/dialog_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_bottom_out</item>
    </style>

</resources>
